{
  "name": "blogging-website",
  "version": "1.0.0",
  "description": "My Blogging Website project",
  "source": "index.html",
  "scripts": {
    "start": "parcel serve index.html",
    "build": "parcel build index.html --no-cache --dist-dir build"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
